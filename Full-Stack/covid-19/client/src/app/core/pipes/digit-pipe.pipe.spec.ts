import { DigitPipePipe } from './digit-pipe.pipe';

describe('DigitPipePipe', () => {
  it('create an instance', () => {
    const pipe = new DigitPipePipe();
    expect(pipe).toBeTruthy();
  });
});
