import { PercentigePipe } from './percentige.pipe';

describe('PercentigePipe', () => {
  it('create an instance', () => {
    const pipe = new PercentigePipe();
    expect(pipe).toBeTruthy();
  });
});
